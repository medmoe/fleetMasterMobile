import React from 'react';
import {SafeAreaView, ScrollView, Text} from 'react-native';

const MaintenanceReport = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>MaintenanceReport</Text>
            </ScrollView>
        </SafeAreaView>
    );
};
export default MaintenanceReport;
